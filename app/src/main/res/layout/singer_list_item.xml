<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    xmlns:tools="http://schemas.android.com/tools">

    <ImageView
        android:id="@+id/singer_image_view"
        android:layout_width="@dimen/singerImageViewWidthAndHeight"
        android:layout_height="@dimen/singerImageViewWidthAndHeight"
        tools:src="@mipmap/ic_launcher"
        android:layout_margin="@dimen/singerImageViewMargin"
        android:contentDescription="@string/singerImageView" />

</RelativeLayout>